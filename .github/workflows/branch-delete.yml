name: Branch deleted
on: delete

jobs:
  delete:
    if: github.event.ref_type == 'branch'
    runs-on: ubuntu-latest
    steps:
      - name: cleanup
        run: |
        DELETED_BRANCH_NAME=$(cat ${{ github.event_path }} | jq --raw-output '.ref')
        echo $DELETED_BRANCH_NAME
